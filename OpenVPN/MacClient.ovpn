remote <remote IP or hostname> <remote port> <protocol>
dev tun
ifconfig 10.42.22.2 10.42.22.1
secret static.key
cipher AES-256-CBC
auth SHA256
comp-lzo

# remote routes
route <remote LAN network address> <remote LAN subnet mask>
